---
phase: 05-cross-linking-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/opengraph-image.tsx
  - apps/web/src/app/hogskoleprovet/opengraph-image.tsx
  - apps/web/src/app/hogskoleprovet/[slug]/opengraph-image.tsx
autonomous: true

must_haves:
  truths:
    - "Root page has OG image for social sharing"
    - "Test list page has OG image for social sharing"
    - "Test detail pages have dynamic OG images with test name"
    - "OG images render with Maxa branding and test information"
  artifacts:
    - path: "apps/web/src/app/opengraph-image.tsx"
      provides: "Root OG image"
      exports: ["default", "alt", "size", "contentType"]
    - path: "apps/web/src/app/hogskoleprovet/opengraph-image.tsx"
      provides: "Test list OG image"
      exports: ["default", "alt", "size", "contentType"]
    - path: "apps/web/src/app/hogskoleprovet/[slug]/opengraph-image.tsx"
      provides: "Dynamic test OG image"
      exports: ["default", "alt", "size", "contentType"]
  key_links:
    - from: "apps/web/src/app/hogskoleprovet/[slug]/opengraph-image.tsx"
      to: "getTestBySlug"
      via: "import from @/data/tests"
      pattern: "import.*getTestBySlug"
---

<objective>
Add OpenGraph images for social sharing using Next.js ImageResponse API.

Purpose: OG images improve social sharing appearance on Twitter, Facebook, LinkedIn. Dynamic images show test-specific content for better click-through rates.
Output: OG images generated for root, test list, and each test detail page.
</objective>

<execution_context>
@/Users/williamlarsten/.claude/get-shit-done/workflows/execute-plan.md
@/Users/williamlarsten/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-cross-linking-polish/05-RESEARCH.md

@apps/web/src/app/hogskoleprovet/[slug]/page.tsx
@apps/web/src/data/tests.ts
@apps/web/src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root and test list OG images</name>
  <files>
    apps/web/src/app/opengraph-image.tsx
    apps/web/src/app/hogskoleprovet/opengraph-image.tsx
  </files>
  <action>
1. Create `apps/web/src/app/opengraph-image.tsx` (root OG image):

```typescript
import { ImageResponse } from "next/og"

export const alt = "Maxa - Hogskoleprovet Prep"
export const size = { width: 1200, height: 630 }
export const contentType = "image/png"

export default function Image() {
  return new ImageResponse(
    (
      <div
        style={{
          height: "100%",
          width: "100%",
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
          justifyContent: "center",
          backgroundColor: "#1E1A2D",
          color: "#F5F5F5",
          fontFamily: "sans-serif",
        }}
      >
        <div style={{ fontSize: 80, fontWeight: "bold", marginBottom: 20 }}>
          Maxa
        </div>
        <div style={{ fontSize: 40, opacity: 0.9 }}>
          Hogskoleprovet Prep
        </div>
        <div
          style={{
            fontSize: 28,
            marginTop: 40,
            opacity: 0.7,
            textAlign: "center",
            maxWidth: 800,
          }}
        >
          Gamla prov, facit och normering
        </div>
      </div>
    ),
    { ...size }
  )
}
```

2. Create `apps/web/src/app/hogskoleprovet/opengraph-image.tsx` (test list OG image):

```typescript
import { ImageResponse } from "next/og"

export const alt = "Gamla Hogskoleprov - Maxa"
export const size = { width: 1200, height: 630 }
export const contentType = "image/png"

export default function Image() {
  return new ImageResponse(
    (
      <div
        style={{
          height: "100%",
          width: "100%",
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
          justifyContent: "center",
          backgroundColor: "#1E1A2D",
          color: "#F5F5F5",
          fontFamily: "sans-serif",
        }}
      >
        <div style={{ fontSize: 72, fontWeight: "bold", marginBottom: 20 }}>
          Gamla Hogskoleprov
        </div>
        <div style={{ fontSize: 36, opacity: 0.9 }}>
          Ladda ner PDF, facit och normering
        </div>
        <div
          style={{
            display: "flex",
            alignItems: "center",
            marginTop: 60,
            gap: 16,
          }}
        >
          <div
            style={{
              backgroundColor: "#D4A017",
              color: "#1E1A2D",
              padding: "12px 32px",
              borderRadius: 12,
              fontSize: 28,
              fontWeight: "bold",
            }}
          >
            Maxa
          </div>
        </div>
      </div>
    ),
    { ...size }
  )
}
```

Design notes:
- Use dark background (#1E1A2D - card-background dark mode)
- Light text (#F5F5F5 - foreground)
- Accent color (#D4A017 - primary gold)
- Flexbox layout only (ImageResponse limitation)
- No external fonts (use system sans-serif for reliability)
  </action>
  <verify>
    - Files exist at correct paths
    - TypeScript compiles: `cd apps/web && bun run build`
    - Check generated images by visiting:
      - http://localhost:3000/opengraph-image (dev mode)
      - Use og:image debugger or inspect meta tags
  </verify>
  <done>
    Root and test list OG images created with Maxa branding.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create dynamic test detail OG images</name>
  <files>
    apps/web/src/app/hogskoleprovet/[slug]/opengraph-image.tsx
  </files>
  <action>
Create `apps/web/src/app/hogskoleprovet/[slug]/opengraph-image.tsx`:

```typescript
import { ImageResponse } from "next/og"
import { getTestBySlug } from "@/data/tests"

export const alt = "Hogskoleprovet - Maxa"
export const size = { width: 1200, height: 630 }
export const contentType = "image/png"

export default async function Image({
  params,
}: {
  params: Promise<{ slug: string }>
}) {
  const { slug } = await params
  const test = getTestBySlug(slug)

  if (!test) {
    // Fallback for unknown tests
    return new ImageResponse(
      (
        <div
          style={{
            height: "100%",
            width: "100%",
            display: "flex",
            alignItems: "center",
            justifyContent: "center",
            backgroundColor: "#1E1A2D",
            color: "#F5F5F5",
            fontSize: 48,
            fontFamily: "sans-serif",
          }}
        >
          Hogskoleprovet - Maxa
        </div>
      ),
      { ...size }
    )
  }

  const seasonLabel = test.season === "var" ? "Varen" : "Hosten"

  return new ImageResponse(
    (
      <div
        style={{
          height: "100%",
          width: "100%",
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
          justifyContent: "center",
          backgroundColor: "#1E1A2D",
          color: "#F5F5F5",
          fontFamily: "sans-serif",
        }}
      >
        <div
          style={{
            fontSize: 36,
            opacity: 0.7,
            marginBottom: 16,
          }}
        >
          Hogskoleprovet
        </div>
        <div style={{ fontSize: 80, fontWeight: "bold", marginBottom: 24 }}>
          {seasonLabel} {test.year}
        </div>
        <div
          style={{
            fontSize: 32,
            opacity: 0.8,
            textAlign: "center",
          }}
        >
          PDF, Facit & Normering
        </div>
        <div
          style={{
            display: "flex",
            alignItems: "center",
            marginTop: 60,
          }}
        >
          <div
            style={{
              backgroundColor: "#D4A017",
              color: "#1E1A2D",
              padding: "12px 32px",
              borderRadius: 12,
              fontSize: 28,
              fontWeight: "bold",
            }}
          >
            Maxa
          </div>
        </div>
      </div>
    ),
    { ...size }
  )
}
```

Key features:
- Async params (Next.js 15 pattern)
- Dynamic content based on test slug
- Fallback for unknown slugs
- Consistent branding with other OG images
- Shows season + year prominently
  </action>
  <verify>
    - File exists at correct path
    - TypeScript compiles
    - Run dev server and check:
      - http://localhost:3000/hogskoleprovet/hosten-2025/opengraph-image
      - Should show "Hosten 2025" with Maxa branding
    - Test Facebook Debugger or Twitter Card Validator with deployed URL
  </verify>
  <done>
    Dynamic test detail OG images created showing test-specific content.
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `cd apps/web && bun run build`
2. OG images accessible at:
   - /opengraph-image (root)
   - /hogskoleprovet/opengraph-image (list)
   - /hogskoleprovet/hosten-2025/opengraph-image (detail)
3. Meta tags present: Inspect page source for og:image tags
4. Images render correctly (1200x630, PNG format)
5. Text is readable and branding is consistent
</verification>

<success_criteria>
- All three OG image routes created and rendering
- Root shows "Maxa" branding
- Test list shows "Gamla Hogskoleprov"
- Test detail shows dynamic "[Season] [Year]" content
- Images use consistent dark theme with gold accent
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-linking-polish/05-03-SUMMARY.md`
</output>
