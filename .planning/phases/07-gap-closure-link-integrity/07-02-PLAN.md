---
phase: 07-gap-closure-link-integrity
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/web/src/app/sitemap.ts
  - apps/web/src/lib/normering/types.ts
autonomous: true

must_haves:
  truths:
    - "Sitemap includes all 5 strategy page URLs"
    - "No outdated TODO comment in normering types"
  artifacts:
    - path: "apps/web/src/app/sitemap.ts"
      provides: "Complete sitemap with strategy routes"
      contains: "strategier"
    - path: "apps/web/src/lib/normering/types.ts"
      provides: "Clean documentation without misleading TODO"
      contains: "DATA SOURCE"
  key_links:
    - from: "sitemap.ts"
      to: "strategy pages"
      via: "URL entries"
      pattern: "hogskoleprovet/strategier"
---

<objective>
Add 5 strategy page URLs to the sitemap and clean up the outdated TODO comment in normering types.

Purpose: Ensures search engines discover all strategy content (currently missing from sitemap.xml) and removes misleading mock data TODO that was resolved in Phase 6.

Output: Updated sitemap.ts with strategy routes at priority 0.85, cleaned types.ts documentation.
</objective>

<execution_context>
@/Users/williamlarsten/.claude/get-shit-done/workflows/execute-plan.md
@/Users/williamlarsten/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/app/sitemap.ts
@apps/web/src/lib/normering/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add strategy routes to sitemap</name>
  <files>apps/web/src/app/sitemap.ts</files>
  <action>
    Edit `apps/web/src/app/sitemap.ts` to add a `strategyRoutes` array between `staticRoutes` and `testRoutes`.

    Add these 5 strategy page URLs:
    1. `${BASE_URL}/hogskoleprovet/strategier` (hub page)
    2. `${BASE_URL}/hogskoleprovet/strategier/kvantitativa-fallor`
    3. `${BASE_URL}/hogskoleprovet/strategier/verbala-fallor`
    4. `${BASE_URL}/hogskoleprovet/strategier/vanliga-misstag`
    5. `${BASE_URL}/hogskoleprovet/strategier/tidsstrategi`

    All strategy routes should use:
    - `lastModified: new Date('2026-01-27')` (date strategy content was created)
    - `changeFrequency: 'monthly'` (evergreen content, rarely changes)
    - `priority: 0.85` (between list page 0.9 and test pages 0.8)

    Update the return statement to include strategy routes:
    `return [...staticRoutes, ...strategyRoutes, ...testRoutes]`
  </action>
  <verify>
    Run the dev server and visit `/sitemap.xml`. Count total URLs — should be 33 (1 homepage + 1 list + 5 strategy + 26 tests). Verify all 5 strategy URLs are present with priority 0.85.
  </verify>
  <done>
    Sitemap contains all 5 strategy page URLs at priority 0.85, total 33 URLs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean up outdated TODO in normering types</name>
  <files>apps/web/src/lib/normering/types.ts</files>
  <action>
    Edit `apps/web/src/lib/normering/types.ts` to replace the outdated TODO comment block (lines 8-13) with accurate documentation.

    Replace:
    ```
     * TODO: Current data in hosten-2025.json is MOCK DATA with realistic distribution.
     * Real data should be extracted from official studera.nu PDFs:
     * - norm{YY}{season}-helaprovet.pdf (total)
     * - norm{YY}{season}-verb.pdf (verbal)
     * - norm{YY}{season}-kvant.pdf (kvantitativ)
    ```

    With:
    ```
     * DATA SOURCE:
     * Normering data extracted from official studera.nu PDFs:
     * - norm{YY}{season}-helaprovet.pdf (total)
     * - norm{YY}{season}-verb.pdf (verbal)
     * - norm{YY}{season}-kvant.pdf (kvantitativ)
     *
     * All data stored in src/data/normering/*.json (26 tests: 2013-2025, both seasons)
    ```

    This replaces the misleading "MOCK DATA" TODO with accurate documentation about the data source, since Phase 6 completed extraction for all 26 tests.
  </action>
  <verify>
    Read the file and confirm no TODO comment remains, and the new DATA SOURCE section is present.
  </verify>
  <done>
    types.ts has accurate DATA SOURCE documentation instead of outdated TODO about mock data.
  </done>
</task>

</tasks>

<verification>
1. `/sitemap.xml` contains exactly 33 URLs
2. All 5 `/hogskoleprovet/strategier/*` URLs present with priority 0.85
3. `types.ts` contains "DATA SOURCE:" not "TODO:"
</verification>

<success_criteria>
- Sitemap has complete coverage of all public pages including strategy content
- No misleading TODO comments remain in normering code
</success_criteria>

<output>
After completion, create `.planning/phases/07-gap-closure-link-integrity/07-02-SUMMARY.md`
</output>
