---
phase: 07-gap-closure-link-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/hogskoleprovet/[slug]/opengraph-image.tsx
autonomous: true

must_haves:
  truths:
    - "OG image generation succeeds for all 26 tests including 2013-2017"
    - "Build completes without Satori flexbox errors"
  artifacts:
    - path: "apps/web/src/app/hogskoleprovet/[slug]/opengraph-image.tsx"
      provides: "Fixed OG image with display:flex on all divs"
      contains: "display: \"flex\""
  key_links:
    - from: "opengraph-image.tsx"
      to: "Satori renderer"
      via: "ImageResponse JSX"
      pattern: "display.*flex"
---

<objective>
Fix the OG image build blocker by adding explicit `display: 'flex'` to all nested div elements in the test page opengraph-image.tsx.

Purpose: Unblocks production deployment. Satori (used by Next.js ImageResponse) requires all multi-child divs to have explicit display:flex or display:none. Currently 4 nested divs are missing this, causing build failures for 8 older tests (2013-2017).

Output: Fixed opengraph-image.tsx that generates OG images for all 26 tests without errors.
</objective>

<execution_context>
@/Users/williamlarsten/.claude/get-shit-done/workflows/execute-plan.md
@/Users/williamlarsten/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/app/hogskoleprovet/[slug]/opengraph-image.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add display:flex to all nested divs in opengraph-image.tsx</name>
  <files>apps/web/src/app/hogskoleprovet/[slug]/opengraph-image.tsx</files>
  <action>
    Open `apps/web/src/app/hogskoleprovet/[slug]/opengraph-image.tsx` and add `display: "flex"` to every div element that is missing it. There are 4 divs that need fixing:

    1. Line 72-79: The "Högskoleprovet" text div — add `display: "flex"` to its style object
    2. Line 81: The season/year text div — add `display: "flex"` to its style object
    3. Line 84-91: The "PDF, Facit & Normering" text div — add `display: "flex"` to its style object
    4. Line 100-108: The "Maxa" button inner div — add `display: "flex"` to its style object

    For text-only divs that stack vertically, just add `display: "flex"`. No flexDirection needed since they contain only text.
    For the Maxa button div, add `display: "flex"` with `alignItems: "center"` and `justifyContent: "center"`.

    Do NOT change any existing styles — only ADD the missing display property.
  </action>
  <verify>
    Run `cd apps/web && npx next build 2>&1 | head -100` and confirm no "Expected div to have explicit display" errors. All 26 test OG images should generate successfully.
  </verify>
  <done>
    All divs in opengraph-image.tsx have explicit `display: "flex"`. Build completes without Satori flexbox errors for any test page.
  </done>
</task>

</tasks>

<verification>
1. `cd apps/web && npx next build` completes without OG image errors
2. No "Expected <div> to have explicit 'display: flex'" errors in build output
3. All 26 test slugs generate OG images at build time
</verification>

<success_criteria>
- Build succeeds without Satori-related errors
- OG images generate for all tests including hosten-2013 through varen-2017
</success_criteria>

<output>
After completion, create `.planning/phases/07-gap-closure-link-integrity/07-01-SUMMARY.md`
</output>
